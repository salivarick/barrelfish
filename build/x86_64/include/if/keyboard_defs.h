#ifndef __keyboard_IF_H
#define __keyboard_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: keyboard
 * INTEFACE FILE: ../if/keyboard.if
 * INTERFACE DESCRIPTION: Keyboard events
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t keyboard_genpaddr_t;
typedef uint64_t keyboard_genvaddr_t;
typedef uint32_t keyboard_rsrcid_t;
typedef uint64_t keyboard_errval_t;
typedef uint64_t keyboard_cycles_t;
typedef uint32_t keyboard_iref_t;
typedef uint8_t keyboard_coreid_t;
typedef uint32_t keyboard_domainid_t;

/*
 * Forward declaration of binding type
 */
struct keyboard_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void keyboard_bind_continuation_fn(void *st, errval_t err, struct keyboard_binding *_binding);
typedef  bool keyboard_can_send_fn(struct keyboard_binding *_binding);
typedef  errval_t keyboard_register_send_fn(struct keyboard_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t keyboard_change_waitset_fn(struct keyboard_binding *_binding, struct waitset *ws);
typedef  errval_t keyboard_control_fn(struct keyboard_binding *_binding, idc_control_t control);
typedef  void keyboard_error_handler_fn(struct keyboard_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum keyboard_msg_enum {
    keyboard___dummy__msgnum = 0,
    keyboard_key_event__msgnum = 1
} keyboard_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t keyboard_key_event__tx_method_fn(struct keyboard_binding *_binding, struct event_closure _continuation, uint8_t scancode, bool extended);

/*
 * Message type signatures (receive)
 */
typedef  void keyboard_key_event__rx_method_fn(struct keyboard_binding *_binding, uint8_t scancode, bool extended);

/*
 * Struct type for holding the args for each msg
 */
struct keyboard_key_event__args {
    uint8_t scancode;
    bool extended;
};

/*
 * Union type for all message arguments
 */
union keyboard_arg_union {
    struct keyboard_key_event__args key_event;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct keyboard_tx_vtbl {
    keyboard_key_event__tx_method_fn *key_event;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct keyboard_rx_vtbl {
    keyboard_key_event__rx_method_fn *key_event;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t keyboard_connect_fn(void *st, struct keyboard_binding *binding);

/*
 * Export state struct
 */
struct keyboard_export {
    struct idc_export common;
    keyboard_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t keyboard_export(void *st, idc_export_callback_fn *export_cb, keyboard_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct keyboard_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    keyboard_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    keyboard_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    keyboard_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    keyboard_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    keyboard_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct keyboard_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct keyboard_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union keyboard_arg_union tx_union;
    union keyboard_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum keyboard_msg_enum tx_msgnum;
    enum keyboard_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    keyboard_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t keyboard_bind(iref_t i, keyboard_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t keyboard_key_event__tx(struct keyboard_binding *_binding, struct event_closure _continuation, uint8_t scancode, bool extended) __attribute__ ((always_inline));
static inline errval_t keyboard_key_event__tx(struct keyboard_binding *_binding, struct event_closure _continuation, uint8_t scancode, bool extended)
{
    return(((_binding->tx_vtbl).key_event)(_binding, _continuation, scancode, extended));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/keyboard_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/keyboard_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/keyboard_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/keyboard_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __keyboard_IF_H
