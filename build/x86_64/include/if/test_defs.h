#ifndef __test_IF_H
#define __test_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: test
 * INTEFACE FILE: ../if/test.if
 * INTERFACE DESCRIPTION: Test interface
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t test_genpaddr_t;
typedef uint64_t test_genvaddr_t;
typedef uint32_t test_rsrcid_t;
typedef uint64_t test_errval_t;
typedef uint64_t test_cycles_t;
typedef uint32_t test_iref_t;
typedef uint8_t test_coreid_t;
typedef uint32_t test_domainid_t;

/*
 * Forward declaration of binding type
 */
struct test_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void test_bind_continuation_fn(void *st, errval_t err, struct test_binding *_binding);
typedef  bool test_can_send_fn(struct test_binding *_binding);
typedef  errval_t test_register_send_fn(struct test_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t test_change_waitset_fn(struct test_binding *_binding, struct waitset *ws);
typedef  errval_t test_control_fn(struct test_binding *_binding, idc_control_t control);
typedef  void test_error_handler_fn(struct test_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum test_msg_enum {
    test___dummy__msgnum = 0,
    test_basic__msgnum = 1,
    test_str__msgnum = 2,
    test_caps__msgnum = 3,
    test_buf__msgnum = 4
} test_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t test_basic__tx_method_fn(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg);
typedef  errval_t test_str__tx_method_fn(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg, const char *s);
typedef  errval_t test_caps__tx_method_fn(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg, struct capref cap1, struct capref cap2);
typedef  errval_t test_buf__tx_method_fn(struct test_binding *_binding, struct event_closure _continuation, const uint8_t *buf, size_t buflen);

/*
 * Message type signatures (receive)
 */
typedef  void test_basic__rx_method_fn(struct test_binding *_binding, uint32_t arg);
typedef  void test_str__rx_method_fn(struct test_binding *_binding, uint32_t arg, char *s);
typedef  void test_caps__rx_method_fn(struct test_binding *_binding, uint32_t arg, struct capref cap1, struct capref cap2);
typedef  void test_buf__rx_method_fn(struct test_binding *_binding, uint8_t *buf, size_t buflen);

/*
 * Struct type for holding the args for each msg
 */
struct test_basic__args {
    uint32_t arg;
};
struct test_str__args {
    uint32_t arg;
    char *s;
};
struct test_caps__args {
    uint32_t arg;
    struct capref cap1;
    struct capref cap2;
};
struct test_buf__args {
    uint8_t *buf;
    size_t buflen;
};

/*
 * Union type for all message arguments
 */
union test_arg_union {
    struct test_basic__args basic;
    struct test_str__args str;
    struct test_caps__args caps;
    struct test_buf__args buf;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct test_tx_vtbl {
    test_basic__tx_method_fn *basic;
    test_str__tx_method_fn *str;
    test_caps__tx_method_fn *caps;
    test_buf__tx_method_fn *buf;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct test_rx_vtbl {
    test_basic__rx_method_fn *basic;
    test_str__rx_method_fn *str;
    test_caps__rx_method_fn *caps;
    test_buf__rx_method_fn *buf;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t test_connect_fn(void *st, struct test_binding *binding);

/*
 * Export state struct
 */
struct test_export {
    struct idc_export common;
    test_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t test_export(void *st, idc_export_callback_fn *export_cb, test_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct test_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    test_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    test_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    test_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    test_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    test_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct test_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct test_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union test_arg_union tx_union;
    union test_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum test_msg_enum tx_msgnum;
    enum test_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    test_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t test_bind(iref_t i, test_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t test_basic__tx(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg) __attribute__ ((always_inline));
static inline errval_t test_basic__tx(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg)
{
    return(((_binding->tx_vtbl).basic)(_binding, _continuation, arg));
}

static inline errval_t test_str__tx(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg, const char *s) __attribute__ ((always_inline));
static inline errval_t test_str__tx(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg, const char *s)
{
    return(((_binding->tx_vtbl).str)(_binding, _continuation, arg, s));
}

static inline errval_t test_caps__tx(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg, struct capref cap1, struct capref cap2) __attribute__ ((always_inline));
static inline errval_t test_caps__tx(struct test_binding *_binding, struct event_closure _continuation, uint32_t arg, struct capref cap1, struct capref cap2)
{
    return(((_binding->tx_vtbl).caps)(_binding, _continuation, arg, cap1, cap2));
}

static inline errval_t test_buf__tx(struct test_binding *_binding, struct event_closure _continuation, const uint8_t *buf, size_t buflen) __attribute__ ((always_inline));
static inline errval_t test_buf__tx(struct test_binding *_binding, struct event_closure _continuation, const uint8_t *buf, size_t buflen)
{
    return(((_binding->tx_vtbl).buf)(_binding, _continuation, buf, buflen));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/test_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/test_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/test_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/test_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __test_IF_H
