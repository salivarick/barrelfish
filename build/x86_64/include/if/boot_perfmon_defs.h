#ifndef __boot_perfmon_IF_H
#define __boot_perfmon_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: boot_perfmon
 * INTEFACE FILE: ../if/boot_perfmon.if
 * INTERFACE DESCRIPTION: The perfmon Interface
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t boot_perfmon_genpaddr_t;
typedef uint64_t boot_perfmon_genvaddr_t;
typedef uint32_t boot_perfmon_rsrcid_t;
typedef uint64_t boot_perfmon_errval_t;
typedef uint64_t boot_perfmon_cycles_t;
typedef uint32_t boot_perfmon_iref_t;
typedef uint8_t boot_perfmon_coreid_t;
typedef uint32_t boot_perfmon_domainid_t;

/*
 * Forward declaration of binding type
 */
struct boot_perfmon_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void boot_perfmon_bind_continuation_fn(void *st, errval_t err, struct boot_perfmon_binding *_binding);
typedef  bool boot_perfmon_can_send_fn(struct boot_perfmon_binding *_binding);
typedef  errval_t boot_perfmon_register_send_fn(struct boot_perfmon_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t boot_perfmon_change_waitset_fn(struct boot_perfmon_binding *_binding, struct waitset *ws);
typedef  errval_t boot_perfmon_control_fn(struct boot_perfmon_binding *_binding, idc_control_t control);
typedef  void boot_perfmon_error_handler_fn(struct boot_perfmon_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum boot_perfmon_msg_enum {
    boot_perfmon___dummy__msgnum = 0,
    boot_perfmon_init__msgnum = 1,
    boot_perfmon_done_request__msgnum = 2,
    boot_perfmon_done_reply__msgnum = 3,
    boot_perfmon_ping__msgnum = 4,
    boot_perfmon_pong__msgnum = 5,
    boot_perfmon_exit__msgnum = 6
} boot_perfmon_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t boot_perfmon_init__tx_method_fn(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_coreid_t id);
typedef  errval_t boot_perfmon_done_request__tx_method_fn(struct boot_perfmon_binding *_binding, struct event_closure _continuation);
typedef  errval_t boot_perfmon_done_reply__tx_method_fn(struct boot_perfmon_binding *_binding, struct event_closure _continuation);
typedef  errval_t boot_perfmon_ping__tx_method_fn(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_cycles_t time);
typedef  errval_t boot_perfmon_pong__tx_method_fn(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_cycles_t time);
typedef  errval_t boot_perfmon_exit__tx_method_fn(struct boot_perfmon_binding *_binding, struct event_closure _continuation);

/*
 * Message type signatures (receive)
 */
typedef  void boot_perfmon_init__rx_method_fn(struct boot_perfmon_binding *_binding, boot_perfmon_coreid_t id);
typedef  void boot_perfmon_done_request__rx_method_fn(struct boot_perfmon_binding *_binding);
typedef  void boot_perfmon_done_reply__rx_method_fn(struct boot_perfmon_binding *_binding);
typedef  void boot_perfmon_ping__rx_method_fn(struct boot_perfmon_binding *_binding, boot_perfmon_cycles_t time);
typedef  void boot_perfmon_pong__rx_method_fn(struct boot_perfmon_binding *_binding, boot_perfmon_cycles_t time);
typedef  void boot_perfmon_exit__rx_method_fn(struct boot_perfmon_binding *_binding);

/*
 * Struct type for holding the args for each msg
 */
struct boot_perfmon_init__args {
    boot_perfmon_coreid_t id;
};
struct boot_perfmon_ping__args {
    boot_perfmon_cycles_t time;
};
struct boot_perfmon_pong__args {
    boot_perfmon_cycles_t time;
};

/*
 * Union type for all message arguments
 */
union boot_perfmon_arg_union {
    struct boot_perfmon_init__args init;
    struct boot_perfmon_ping__args ping;
    struct boot_perfmon_pong__args pong;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct boot_perfmon_tx_vtbl {
    boot_perfmon_init__tx_method_fn *init;
    boot_perfmon_done_request__tx_method_fn *done_request;
    boot_perfmon_done_reply__tx_method_fn *done_reply;
    boot_perfmon_ping__tx_method_fn *ping;
    boot_perfmon_pong__tx_method_fn *pong;
    boot_perfmon_exit__tx_method_fn *exit;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct boot_perfmon_rx_vtbl {
    boot_perfmon_init__rx_method_fn *init;
    boot_perfmon_done_request__rx_method_fn *done_request;
    boot_perfmon_done_reply__rx_method_fn *done_reply;
    boot_perfmon_ping__rx_method_fn *ping;
    boot_perfmon_pong__rx_method_fn *pong;
    boot_perfmon_exit__rx_method_fn *exit;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t boot_perfmon_connect_fn(void *st, struct boot_perfmon_binding *binding);

/*
 * Export state struct
 */
struct boot_perfmon_export {
    struct idc_export common;
    boot_perfmon_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t boot_perfmon_export(void *st, idc_export_callback_fn *export_cb, boot_perfmon_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct boot_perfmon_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    boot_perfmon_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    boot_perfmon_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    boot_perfmon_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    boot_perfmon_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    boot_perfmon_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct boot_perfmon_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct boot_perfmon_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union boot_perfmon_arg_union tx_union;
    union boot_perfmon_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum boot_perfmon_msg_enum tx_msgnum;
    enum boot_perfmon_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    boot_perfmon_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t boot_perfmon_bind(iref_t i, boot_perfmon_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t boot_perfmon_init__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_coreid_t id) __attribute__ ((always_inline));
static inline errval_t boot_perfmon_init__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_coreid_t id)
{
    return(((_binding->tx_vtbl).init)(_binding, _continuation, id));
}

static inline errval_t boot_perfmon_done_request__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t boot_perfmon_done_request__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).done_request)(_binding, _continuation));
}

static inline errval_t boot_perfmon_done_reply__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t boot_perfmon_done_reply__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).done_reply)(_binding, _continuation));
}

static inline errval_t boot_perfmon_ping__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_cycles_t time) __attribute__ ((always_inline));
static inline errval_t boot_perfmon_ping__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_cycles_t time)
{
    return(((_binding->tx_vtbl).ping)(_binding, _continuation, time));
}

static inline errval_t boot_perfmon_pong__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_cycles_t time) __attribute__ ((always_inline));
static inline errval_t boot_perfmon_pong__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation, boot_perfmon_cycles_t time)
{
    return(((_binding->tx_vtbl).pong)(_binding, _continuation, time));
}

static inline errval_t boot_perfmon_exit__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t boot_perfmon_exit__tx(struct boot_perfmon_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).exit)(_binding, _continuation));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/boot_perfmon_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/boot_perfmon_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/boot_perfmon_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/boot_perfmon_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __boot_perfmon_IF_H
