#ifndef __terminal_config_IF_H
#define __terminal_config_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: terminal_config
 * INTEFACE FILE: ../if/terminal_config.if
 * INTERFACE DESCRIPTION: Terminal Configuration Interface
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t terminal_config_genpaddr_t;
typedef uint64_t terminal_config_genvaddr_t;
typedef uint32_t terminal_config_rsrcid_t;
typedef uint64_t terminal_config_errval_t;
typedef uint64_t terminal_config_cycles_t;
typedef uint32_t terminal_config_iref_t;
typedef uint8_t terminal_config_coreid_t;
typedef uint32_t terminal_config_domainid_t;
typedef uint32_t terminal_config_option_t;

/*
 * Forward declaration of binding type
 */
struct terminal_config_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void terminal_config_bind_continuation_fn(void *st, errval_t err, struct terminal_config_binding *_binding);
typedef  bool terminal_config_can_send_fn(struct terminal_config_binding *_binding);
typedef  errval_t terminal_config_register_send_fn(struct terminal_config_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t terminal_config_change_waitset_fn(struct terminal_config_binding *_binding, struct waitset *ws);
typedef  errval_t terminal_config_control_fn(struct terminal_config_binding *_binding, idc_control_t control);
typedef  void terminal_config_error_handler_fn(struct terminal_config_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum terminal_config_msg_enum {
    terminal_config___dummy__msgnum = 0,
    terminal_config_configuration__msgnum = 1,
    terminal_config_disconnect__msgnum = 2
} terminal_config_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t terminal_config_configuration__tx_method_fn(struct terminal_config_binding *_binding, struct event_closure _continuation, terminal_config_option_t opt, const char *argument);
typedef  errval_t terminal_config_disconnect__tx_method_fn(struct terminal_config_binding *_binding, struct event_closure _continuation);

/*
 * Message type signatures (receive)
 */
typedef  void terminal_config_configuration__rx_method_fn(struct terminal_config_binding *_binding, terminal_config_option_t opt, char *argument);
typedef  void terminal_config_disconnect__rx_method_fn(struct terminal_config_binding *_binding);

/*
 * Struct type for holding the args for each msg
 */
struct terminal_config_configuration__args {
    terminal_config_option_t opt;
    char *argument;
};

/*
 * Union type for all message arguments
 */
union terminal_config_arg_union {
    struct terminal_config_configuration__args configuration;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct terminal_config_tx_vtbl {
    terminal_config_configuration__tx_method_fn *configuration;
    terminal_config_disconnect__tx_method_fn *disconnect;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct terminal_config_rx_vtbl {
    terminal_config_configuration__rx_method_fn *configuration;
    terminal_config_disconnect__rx_method_fn *disconnect;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t terminal_config_connect_fn(void *st, struct terminal_config_binding *binding);

/*
 * Export state struct
 */
struct terminal_config_export {
    struct idc_export common;
    terminal_config_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t terminal_config_export(void *st, idc_export_callback_fn *export_cb, terminal_config_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct terminal_config_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    terminal_config_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    terminal_config_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    terminal_config_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    terminal_config_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    terminal_config_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct terminal_config_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct terminal_config_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union terminal_config_arg_union tx_union;
    union terminal_config_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum terminal_config_msg_enum tx_msgnum;
    enum terminal_config_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    terminal_config_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t terminal_config_bind(iref_t i, terminal_config_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t terminal_config_configuration__tx(struct terminal_config_binding *_binding, struct event_closure _continuation, terminal_config_option_t opt, const char *argument) __attribute__ ((always_inline));
static inline errval_t terminal_config_configuration__tx(struct terminal_config_binding *_binding, struct event_closure _continuation, terminal_config_option_t opt, const char *argument)
{
    return(((_binding->tx_vtbl).configuration)(_binding, _continuation, opt, argument));
}

static inline errval_t terminal_config_disconnect__tx(struct terminal_config_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t terminal_config_disconnect__tx(struct terminal_config_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).disconnect)(_binding, _continuation));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/terminal_config_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/terminal_config_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/terminal_config_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/terminal_config_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __terminal_config_IF_H
