#ifndef __xcorecap_IF_H
#define __xcorecap_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: xcorecap
 * INTEFACE FILE: ../if/xcorecap.if
 * INTERFACE DESCRIPTION: Cross core capability test interface
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t xcorecap_genpaddr_t;
typedef uint64_t xcorecap_genvaddr_t;
typedef uint32_t xcorecap_rsrcid_t;
typedef uint64_t xcorecap_errval_t;
typedef uint64_t xcorecap_cycles_t;
typedef uint32_t xcorecap_iref_t;
typedef uint8_t xcorecap_coreid_t;
typedef uint32_t xcorecap_domainid_t;

/*
 * Forward declaration of binding type
 */
struct xcorecap_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void xcorecap_bind_continuation_fn(void *st, errval_t err, struct xcorecap_binding *_binding);
typedef  bool xcorecap_can_send_fn(struct xcorecap_binding *_binding);
typedef  errval_t xcorecap_register_send_fn(struct xcorecap_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t xcorecap_change_waitset_fn(struct xcorecap_binding *_binding, struct waitset *ws);
typedef  errval_t xcorecap_control_fn(struct xcorecap_binding *_binding, idc_control_t control);
typedef  void xcorecap_error_handler_fn(struct xcorecap_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum xcorecap_msg_enum {
    xcorecap___dummy__msgnum = 0,
    xcorecap_send_cap__msgnum = 1,
    xcorecap_retype_cap__msgnum = 2,
    xcorecap_delete_cap__msgnum = 3,
    xcorecap_revoke_cap__msgnum = 4,
    xcorecap_send_done__msgnum = 5
} xcorecap_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t xcorecap_send_cap__tx_method_fn(struct xcorecap_binding *_binding, struct event_closure _continuation, struct capref cap);
typedef  errval_t xcorecap_retype_cap__tx_method_fn(struct xcorecap_binding *_binding, struct event_closure _continuation);
typedef  errval_t xcorecap_delete_cap__tx_method_fn(struct xcorecap_binding *_binding, struct event_closure _continuation);
typedef  errval_t xcorecap_revoke_cap__tx_method_fn(struct xcorecap_binding *_binding, struct event_closure _continuation);
typedef  errval_t xcorecap_send_done__tx_method_fn(struct xcorecap_binding *_binding, struct event_closure _continuation);

/*
 * Message type signatures (receive)
 */
typedef  void xcorecap_send_cap__rx_method_fn(struct xcorecap_binding *_binding, struct capref cap);
typedef  void xcorecap_retype_cap__rx_method_fn(struct xcorecap_binding *_binding);
typedef  void xcorecap_delete_cap__rx_method_fn(struct xcorecap_binding *_binding);
typedef  void xcorecap_revoke_cap__rx_method_fn(struct xcorecap_binding *_binding);
typedef  void xcorecap_send_done__rx_method_fn(struct xcorecap_binding *_binding);

/*
 * Struct type for holding the args for each msg
 */
struct xcorecap_send_cap__args {
    struct capref cap;
};

/*
 * Union type for all message arguments
 */
union xcorecap_arg_union {
    struct xcorecap_send_cap__args send_cap;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct xcorecap_tx_vtbl {
    xcorecap_send_cap__tx_method_fn *send_cap;
    xcorecap_retype_cap__tx_method_fn *retype_cap;
    xcorecap_delete_cap__tx_method_fn *delete_cap;
    xcorecap_revoke_cap__tx_method_fn *revoke_cap;
    xcorecap_send_done__tx_method_fn *send_done;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct xcorecap_rx_vtbl {
    xcorecap_send_cap__rx_method_fn *send_cap;
    xcorecap_retype_cap__rx_method_fn *retype_cap;
    xcorecap_delete_cap__rx_method_fn *delete_cap;
    xcorecap_revoke_cap__rx_method_fn *revoke_cap;
    xcorecap_send_done__rx_method_fn *send_done;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t xcorecap_connect_fn(void *st, struct xcorecap_binding *binding);

/*
 * Export state struct
 */
struct xcorecap_export {
    struct idc_export common;
    xcorecap_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t xcorecap_export(void *st, idc_export_callback_fn *export_cb, xcorecap_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct xcorecap_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    xcorecap_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    xcorecap_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    xcorecap_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    xcorecap_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    xcorecap_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct xcorecap_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct xcorecap_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union xcorecap_arg_union tx_union;
    union xcorecap_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum xcorecap_msg_enum tx_msgnum;
    enum xcorecap_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    xcorecap_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t xcorecap_bind(iref_t i, xcorecap_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t xcorecap_send_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation, struct capref cap) __attribute__ ((always_inline));
static inline errval_t xcorecap_send_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation, struct capref cap)
{
    return(((_binding->tx_vtbl).send_cap)(_binding, _continuation, cap));
}

static inline errval_t xcorecap_retype_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t xcorecap_retype_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).retype_cap)(_binding, _continuation));
}

static inline errval_t xcorecap_delete_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t xcorecap_delete_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).delete_cap)(_binding, _continuation));
}

static inline errval_t xcorecap_revoke_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t xcorecap_revoke_cap__tx(struct xcorecap_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).revoke_cap)(_binding, _continuation));
}

static inline errval_t xcorecap_send_done__tx(struct xcorecap_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t xcorecap_send_done__tx(struct xcorecap_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).send_done)(_binding, _continuation));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/xcorecap_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/xcorecap_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/xcorecap_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/xcorecap_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __xcorecap_IF_H
