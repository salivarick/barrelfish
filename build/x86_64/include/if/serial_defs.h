#ifndef __serial_IF_H
#define __serial_IF_H 1
/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: serial
 * INTEFACE FILE: ../if/serial.if
 * INTERFACE DESCRIPTION: Serial characters
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

#include <flounder/flounder.h>

/*
 * Concrete type definitions
 */
typedef uint64_t serial_genpaddr_t;
typedef uint64_t serial_genvaddr_t;
typedef uint32_t serial_rsrcid_t;
typedef uint64_t serial_errval_t;
typedef uint64_t serial_cycles_t;
typedef uint32_t serial_iref_t;
typedef uint8_t serial_coreid_t;
typedef uint32_t serial_domainid_t;

/*
 * Forward declaration of binding type
 */
struct serial_binding;

/*
 * Contination (callback) and control function types
 */
typedef  void serial_bind_continuation_fn(void *st, errval_t err, struct serial_binding *_binding);
typedef  bool serial_can_send_fn(struct serial_binding *_binding);
typedef  errval_t serial_register_send_fn(struct serial_binding *_binding, struct waitset *ws, struct event_closure _continuation);
typedef  errval_t serial_change_waitset_fn(struct serial_binding *_binding, struct waitset *ws);
typedef  errval_t serial_control_fn(struct serial_binding *_binding, idc_control_t control);
typedef  void serial_error_handler_fn(struct serial_binding *_binding, errval_t err);

/*
 * Enumeration for message numbers
 */
typedef enum serial_msg_enum {
    serial___dummy__msgnum = 0,
    serial_output__msgnum = 1,
    serial_input__msgnum = 2,
    serial_associate_stdin__msgnum = 3
} serial_msg_enum;

/*
 * Message type signatures (transmit)
 */
typedef  errval_t serial_output__tx_method_fn(struct serial_binding *_binding, struct event_closure _continuation, const char *data, size_t len);
typedef  errval_t serial_input__tx_method_fn(struct serial_binding *_binding, struct event_closure _continuation, const char *data, size_t len);
typedef  errval_t serial_associate_stdin__tx_method_fn(struct serial_binding *_binding, struct event_closure _continuation);

/*
 * Message type signatures (receive)
 */
typedef  void serial_output__rx_method_fn(struct serial_binding *_binding, char *data, size_t len);
typedef  void serial_input__rx_method_fn(struct serial_binding *_binding, char *data, size_t len);
typedef  void serial_associate_stdin__rx_method_fn(struct serial_binding *_binding);

/*
 * Struct type for holding the args for each msg
 */
struct serial_output__args {
    char *data;
    size_t len;
};
struct serial_input__args {
    char *data;
    size_t len;
};

/*
 * Union type for all message arguments
 */
union serial_arg_union {
    struct serial_output__args output;
    struct serial_input__args input;
};

/*
 * VTable struct definition for the interface (transmit)
 */
struct serial_tx_vtbl {
    serial_output__tx_method_fn *output;
    serial_input__tx_method_fn *input;
    serial_associate_stdin__tx_method_fn *associate_stdin;
};

/*
 * VTable struct definition for the interface (receive)
 */
struct serial_rx_vtbl {
    serial_output__rx_method_fn *output;
    serial_input__rx_method_fn *input;
    serial_associate_stdin__rx_method_fn *associate_stdin;
};

/*
 * Incoming connect callback type
 */
typedef  errval_t serial_connect_fn(void *st, struct serial_binding *binding);

/*
 * Export state struct
 */
struct serial_export {
    struct idc_export common;
    serial_connect_fn *connect_cb;
    struct waitset *waitset;
    void *st;
};

/*
 * Export function
 */
extern  errval_t serial_export(void *st, idc_export_callback_fn *export_cb, serial_connect_fn *connect_cb, struct waitset *ws, idc_export_flags_t flags);

/*
 * The Binding structure
 */
struct serial_binding {
    /* Arbitrary user state pointer */
    void *st;
    
    /* Waitset used for receive handlers and send continuations */
    struct waitset *waitset;
    
    /* Mutex for the use of user code. */
    /* Must be held before any operation where there is a possibility of */
    /* concurrent access to the same binding (eg. multiple threads, or */
    /* asynchronous event handlers that use the same binding object). */
    struct event_mutex mutex;
    
    /* returns true iff a message could currently be accepted by the binding */
    serial_can_send_fn *can_send;
    
    /* register an event for when a message is likely to be able to be sent */
    serial_register_send_fn *register_send;
    
    /* change the waitset used by a binding */
    serial_change_waitset_fn *change_waitset;
    
    /* perform control operations */
    serial_control_fn *control;
    
    /* error handler for any async errors associated with this binding */
    /* must be filled-in by user */
    serial_error_handler_fn *error_handler;
    
    /* Message send functions (filled in by binding) */
    struct serial_tx_vtbl tx_vtbl;
    
    /* Incoming message handlers (filled in by user) */
    struct serial_rx_vtbl rx_vtbl;
    
    /* Private state belonging to the binding implementation */
    union serial_arg_union tx_union;
    union serial_arg_union rx_union;
    struct waitset_chanstate register_chanstate;
    struct waitset_chanstate tx_cont_chanstate;
    enum serial_msg_enum tx_msgnum;
    enum serial_msg_enum rx_msgnum;
    int tx_msg_fragment;
    int rx_msg_fragment;
    size_t tx_str_pos;
    size_t rx_str_pos;
    size_t tx_str_len;
    size_t rx_str_len;
    struct event_queue_node event_qnode;
    serial_bind_continuation_fn *bind_cont;
};

/*
 * Generic bind function
 */
extern  errval_t serial_bind(iref_t i, serial_bind_continuation_fn *_continuation, void *st, struct waitset *waitset, idc_bind_flags_t flags);

/*
 * Send wrappers
 */
static inline errval_t serial_output__tx(struct serial_binding *_binding, struct event_closure _continuation, const char *data, size_t len) __attribute__ ((always_inline));
static inline errval_t serial_output__tx(struct serial_binding *_binding, struct event_closure _continuation, const char *data, size_t len)
{
    return(((_binding->tx_vtbl).output)(_binding, _continuation, data, len));
}

static inline errval_t serial_input__tx(struct serial_binding *_binding, struct event_closure _continuation, const char *data, size_t len) __attribute__ ((always_inline));
static inline errval_t serial_input__tx(struct serial_binding *_binding, struct event_closure _continuation, const char *data, size_t len)
{
    return(((_binding->tx_vtbl).input)(_binding, _continuation, data, len));
}

static inline errval_t serial_associate_stdin__tx(struct serial_binding *_binding, struct event_closure _continuation) __attribute__ ((always_inline));
static inline errval_t serial_associate_stdin__tx(struct serial_binding *_binding, struct event_closure _continuation)
{
    return(((_binding->tx_vtbl).associate_stdin)(_binding, _continuation));
}


/*
 * Backend-specific includes
 */
#ifdef CONFIG_FLOUNDER_BACKEND_LMP
#include <if/serial_lmp_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_LMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP
#include <if/serial_ump_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP
#ifdef CONFIG_FLOUNDER_BACKEND_UMP_IPI
#include <if/serial_ump_ipi_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_UMP_IPI
#ifdef CONFIG_FLOUNDER_BACKEND_MULTIHOP
#include <if/serial_multihop_defs.h>
#endif // CONFIG_FLOUNDER_BACKEND_MULTIHOP
/*
 * And we're done
 */
#endif // __serial_IF_H
