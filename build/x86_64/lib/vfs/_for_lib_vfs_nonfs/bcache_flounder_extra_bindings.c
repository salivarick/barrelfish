/*
 * Copyright (c) 2010, ETH Zurich.
 * All rights reserved.
 * 
 * INTERFACE NAME: bcache
 * INTEFACE FILE: ../if/bcache.if
 * INTERFACE DESCRIPTION: Buffer cache
 * 
 * This file is distributed under the terms in the attached LICENSE
 * file. If you do not find this file, copies can be found by
 * writing to:
 * ETH Zurich D-INFK, Universitaetstr.6, CH-8092 Zurich.
 * Attn: Systems Group.
 * 
 * THIS FILE IS AUTOMATICALLY GENERATED BY FLOUNDER: DO NOT EDIT!
 */

/*
 * Generated Stub for RPC
 */

#include <barrelfish/barrelfish.h>
#include <flounder/flounder_support.h>
#include <if/bcache_rpcclient_defs.h>

/*
 * RPC wrapper functions
 */
static  errval_t bcache_new_client__rpc(struct bcache_rpc_client *_rpc, struct capref *bulk)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).new_client_call)(_rpc->b, NOP_CONT));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    // grab reply variables out of binding
    struct bcache_binding *_binding = _rpc->b;
    *bulk = (((_binding->rx_union).new_client_response).bulk);
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}

static  errval_t bcache_get_start__rpc(struct bcache_rpc_client *_rpc, const char *key, size_t key_len, uint64_t *idx, bool *haveit, uint64_t *transid, uint64_t *size)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).get_start_call)(_rpc->b, NOP_CONT, key, key_len));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    // grab reply variables out of binding
    struct bcache_binding *_binding = _rpc->b;
    *idx = (((_binding->rx_union).get_start_response).idx);
    *haveit = (((_binding->rx_union).get_start_response).haveit);
    *transid = (((_binding->rx_union).get_start_response).transid);
    *size = (((_binding->rx_union).get_start_response).size);
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}

static  errval_t bcache_get_stop__rpc(struct bcache_rpc_client *_rpc, uint64_t transid, uint64_t idx, uint64_t length)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).get_stop_call)(_rpc->b, NOP_CONT, transid, idx, length));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}

static  errval_t bcache_print_stats__rpc(struct bcache_rpc_client *_rpc)
{
    errval_t _err = SYS_ERR_OK;
    assert(!(_rpc->rpc_in_progress));
    assert((_rpc->async_error) == SYS_ERR_OK);
    _rpc->rpc_in_progress = true;
    _rpc->reply_present = false;
    
    // call send function
    _err = ((((_rpc->b)->tx_vtbl).print_stats_call)(_rpc->b, NOP_CONT));
    if (err_is_fail(_err)) {
        goto out;
    }
    
    // wait for message to be sent and reply or error to be present
    while (((!(_rpc->reply_present)) || (!(((_rpc->b)->can_send)(_rpc->b)))) && ((_rpc->async_error) == SYS_ERR_OK)) {
        _err = event_dispatch(&(_rpc->rpc_waitset));
        if (err_is_fail(_err)) {
            _err = err_push(_err, LIB_ERR_EVENT_DISPATCH);
            goto out;
        }
    }
    
    if (err_is_fail(_rpc->async_error)) {
        _err = (_rpc->async_error);
        _rpc->async_error = SYS_ERR_OK;
        goto out;
    }
    
    out:
    _rpc->rpc_in_progress = false;
    return(_err);
}


/*
 * Receive handlers
 */
static  void bcache_new_client__rpc_rx_handler(struct bcache_binding *_binding, struct capref bulk)
{
    // get RPC client state pointer
    struct bcache_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    ((_binding->rx_union).new_client_response).bulk = bulk;
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}

static  void bcache_get_start__rpc_rx_handler(struct bcache_binding *_binding, uint64_t idx, bool haveit, uint64_t transid, uint64_t size)
{
    // get RPC client state pointer
    struct bcache_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    ((_binding->rx_union).get_start_response).idx = idx;
    ((_binding->rx_union).get_start_response).haveit = haveit;
    ((_binding->rx_union).get_start_response).transid = transid;
    ((_binding->rx_union).get_start_response).size = size;
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}

static  void bcache_get_stop__rpc_rx_handler(struct bcache_binding *_binding)
{
    // get RPC client state pointer
    struct bcache_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}

static  void bcache_print_stats__rpc_rx_handler(struct bcache_binding *_binding)
{
    // get RPC client state pointer
    struct bcache_rpc_client *_rpc = _binding->st;
    
    // XXX: stash reply parameters in binding object
    // depending on the interconnect driver, they're probably already there
    
    // notify RPC function, and we're done
    _rpc->reply_present = true;
}


/*
 * RPC Vtable
 */
static  struct bcache_rpc_vtbl bcache_rpc_vtbl = {
    .new_client = bcache_new_client__rpc,
    .get_start = bcache_get_start__rpc,
    .get_stop = bcache_get_stop__rpc,
    .print_stats = bcache_print_stats__rpc,
};

/*
 * Error handler
 */
static  void bcache_rpc_client_error(struct bcache_binding *_binding, errval_t _err)
{
    // get RPC client state pointer
    struct bcache_rpc_client *_rpc = _binding->st;
    
    if (_rpc->rpc_in_progress) {
        assert(err_is_fail(_err));
        _rpc->async_error = _err;
        // kick waitset with dummy event
        flounder_support_register(&(_rpc->rpc_waitset), &(_rpc->dummy_chanstate), dummy_event_closure, true);
    } else {
        USER_PANIC_ERR(_err, "async error in RPC");
    }
}


/*
 * Init function
 */
 errval_t bcache_rpc_client_init(struct bcache_rpc_client *rpc, struct bcache_binding *binding)
{
    errval_t _err;
    
    // Setup state of RPC client object
    rpc->b = binding;
    rpc->reply_present = false;
    rpc->rpc_in_progress = false;
    rpc->async_error = SYS_ERR_OK;
    waitset_init(&(rpc->rpc_waitset));
    flounder_support_waitset_chanstate_init(&(rpc->dummy_chanstate));
    rpc->vtbl = bcache_rpc_vtbl;
    binding->st = rpc;
    
    // Change waitset on binding
    _err = ((binding->change_waitset)(binding, &(rpc->rpc_waitset)));
    if (err_is_fail(_err)) {
        waitset_destroy(&(rpc->rpc_waitset));
        return(err_push(_err, FLOUNDER_ERR_CHANGE_WAITSET));
    }
    
    // Set RX handlers on binding object for RPCs
    (binding->rx_vtbl).new_client_response = bcache_new_client__rpc_rx_handler;
    (binding->rx_vtbl).get_start_response = bcache_get_start__rpc_rx_handler;
    (binding->rx_vtbl).get_stop_response = bcache_get_stop__rpc_rx_handler;
    (binding->rx_vtbl).print_stats_response = bcache_print_stats__rpc_rx_handler;
    
    // Set error handler on binding object
    binding->error_handler = bcache_rpc_client_error;
    
    return(SYS_ERR_OK);
}

